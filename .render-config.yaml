# .render-config.yaml — DEPRECATED: Render deployment configuration removed during rollback
# Keep this file only for historical reference. The project has been reverted to local development settings.
